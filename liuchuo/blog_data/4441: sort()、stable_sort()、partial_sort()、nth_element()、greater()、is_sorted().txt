<p>sort(a, a+5); // 默认从小到大,int数组的排序<br>sort(v.begin(), v.end()); // vector数组排序<br>sort中使用的是快排和插排</p><p>stable_sort(v.begin(), v.end());</p><p>partial_sort(a, a+2, a+n); // 前2个数字将是正确顺序<br>partial_sort(v.begin(), v.begin() + 3, v.end()); // 对数组中元素部分排序，前3个将是正确顺序</p><p>nth_element(v.begin(), v.begin() + 6, v.end()); // 只有v[6]处放置的是正确的元素，其余不管～</p><p>sort(v.begin(), v.end(), greater&lt;int&gt;()); // 从大到小排，algorithm头文件</p><p>bool flag = is_sorted(a, a+n);<br>bool flag = is_sort(v.begin(), v.end());</p>