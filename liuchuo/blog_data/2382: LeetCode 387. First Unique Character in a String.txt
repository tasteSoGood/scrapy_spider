<p>Given a string, find the first non-repeating character in it and return it’s index. If it doesn’t exist, return -1.</p><p>Examples:</p><p>s = “leetcode”<br>return 0.</p><p>s = “loveleetcode”,<br>return 2.<br>Note: You may assume the string contain only lowercase letters.</p><p><span style="color: #ff9900;"><strong>分析：map存储每个字符的出现次数，从头遍历数组return第一个m[s[i]] == 1的下标。如果没有就return -1。</strong></span></p><p> </p>